global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'phoenix-ml-platform'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:8000'] # Assuming API runs on host for dev, or use api container name
